# Matrix client library with E2E encryption support
matrix-nio>=0.25.0

# E2E encryption dependencies
# matrix-nio 0.25.2 requires python-olm to enable ENCRYPTION_ENABLED flag
# but uses vodozemac for actual encryption when available (better performance)
python-olm>=3.2.0
vodozemac>=0.9.0

# HTTP client for REST API calls
aiohttp>=3.10.0

# Human JSON (JSON with comments) for configuration files
hjson>=3.1.0

# Colored logging output
colorlog>=6.7.0

# TUI library for interactive interface
textual>=0.47.0

# QR code generation for device verification
qrcode>=7.4.2

# YAML parsing for tests
PyYAML>=6.0

# System and process utilities for info command
psutil>=5.9.0

# Additional dependencies
cachetools>=5.3.0
peewee>=3.17.0
atomicwrites>=1.4.1




